import BilamoHome from './BilamoHome'

/**
 * Bilamo production home — conversation-first orb experience.
 * Legacy travel-booking home layouts are retired from the product path.
 */
export default function Home() {
  return <BilamoHome />
}

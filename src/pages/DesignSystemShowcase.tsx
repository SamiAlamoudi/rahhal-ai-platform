/**
 * Public design-system showcase route.
 * UI foundation only — no auth, APIs, or business logic.
 */

import { DesignSystemGallery } from '../design-system'

export default function DesignSystemShowcase() {
  return <DesignSystemGallery />
}

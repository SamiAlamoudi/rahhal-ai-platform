# ai/reasoning

## Responsibilities

Agent decision helpers plus shared decision-score and reasoning engines.

## Public API

- `src/lib/agent/decision`
- `src/utils/reasoningEngine`
- `src/utils/decisionScoreEngine`

## Dependencies

`core` engines (same sources). No UI.

## Rules

- Compatibility shim only; engines remain in `utils/` until moved.

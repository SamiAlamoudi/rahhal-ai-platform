# auth

## Responsibilities

Authentication helpers and session-related auth utilities.

## Public API

- `src/lib/auth`

## Dependencies

May use `shared`, `infrastructure` (Supabase). Must not import UI.

## Rules

- Compatibility shim only.

# Domains

Domain-driven scaffolding for Bilamo. These folders are **compatibility shims**: they re-export existing implementations from `src/lib/*`, `src/utils/*`, and `src/integrations/*` without moving code or changing runtime behavior.

See `docs/DOMAIN_NAMING_POLICY.md` for brand vs domain naming boundaries.
